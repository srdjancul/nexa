export default function Home() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-[1400px] flex-col justify-center px-5 py-24">
      <p className="text-ink-faint font-mono text-[10px] tracking-[0.2em] uppercase">
        Nexa — scaffold
      </p>
      <h1 className="text-ink mt-6 text-5xl leading-[0.95] font-medium tracking-tight sm:text-7xl">
        Nothing here yet.
      </h1>
      <p className="text-ink-muted mt-6 max-w-[48ch] text-lg">
        Next.js App Router, TypeScript and Tailwind are wired up. Sections and
        copy land once the product definition is settled.
      </p>
    </main>
  );
}
